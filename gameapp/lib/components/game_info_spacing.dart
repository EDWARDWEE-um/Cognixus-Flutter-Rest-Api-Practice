import 'package:flutter/material.dart';

class GameInfoSpacing extends StatelessWidget {
  const GameInfoSpacing({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 4,
    );
  }
}
